{
  "version": "0.2.0",
  "configurations": [
    {
      // F5 always builds and debugs the scratchpad program in Debug mode.
      "name": "Build and debug bootcamp1",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/bootcamp1",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}/build",
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "Configure and build bootcamp1 (Debug)"
    }
  ]
}
